/// Constants used across waveform visualizations
class WaveformConstants {
  WaveformConstants._();

  // Layout dimensions
  static const double waveformHeightRatio = 0.5;
  static const double waveformTopRatio = 0.25;
  static const double padding = 20.0;
  static const double cutWidth = 8.0;

  // Height multipliers for weight visualization
  static const double fullHeightMultiplier = 1.0;
  static const double reducedHeightMultiplier = 0.75;

  // Paint properties
  static const double strokeWidth = 2.5;
  static const double cutSymbolStrokeWidth = 2.0;

  // Cut symbol dimensions
  static const double cutLineSpacing = 4.0;
  static const double cutLineLengthRatio = 0.1;
  static const double cutHorizontalOffset = 2.0;

  // Text sizes
  static const double labelFontSize = 11;
  static const double timeFontSize = 10;
}
